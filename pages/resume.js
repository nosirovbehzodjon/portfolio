import TextGredient from "../src/components/text/textGredient";

const Resume = () => {
    return (
        <header>
            <TextGredient
                textData={{
                    text: "resume",
                    type: "large",
                }}
            />
        </header>
    );
};
export default Resume;
